# Browser-facing API base. Keep as localhost:3000 to use the Next.js proxy (fixes cross-origin cookies).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Server-side only — real backend URL (VM or local). Used by next.config.ts rewrites.
API_UPSTREAM_URL=http://localhost:8000